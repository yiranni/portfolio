{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Yiran&#39;s Portfolio",
  "author": "Yiran Ni",
  "private": false,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "element-ui": "^2.4.11"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
